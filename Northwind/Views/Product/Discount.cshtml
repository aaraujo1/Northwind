@using Northwind.DataLayer

@{
    ViewBag.Title = "Discount";
}
<h2>Discount</h2>

@*<div class="row">*@

<ul class="list-group">

    @foreach (Discount d in @Model)
    {
        @*<div class="col-sm-4">
                <h2 class="font-md">@d.Title</h2>
                <hr class="hr-special" />
                <p>
                    @d.Description
                </p>
                <p><i>Discount Expires: @string.Format("{0:MM/dd/yyyy}", @d.EndTime)</i></p>
                <a class="special" data-container="body" data-toggle="popover" dataplacement="right"
                   data-content="@d.Code">
                    <i class="fa fa-hand-o-right"></i> Discount Code
                </a>
            </div>*@

        <li class="list-group-item">
            <h4 class="list-group-item-heading">@d.Title</h4>
            <p class="list-group-item-text">
                @d.Description
                <h5><i>Discount Expires: @string.Format("{0:MM/dd/yyyy}", @d.EndTime)</i></h5>
                <a class="special" data-container="body" data-toggle="popover" data-placement="right" data-content="@d.Code">
                    <i class="glyphicon glyphicon-bullhorn"></i> Discount Code
                </a>
            </p>
        </li>

    }
@*</div>*@

</ul>

<div class="font-md">
        <a href="~/Home/Index"><i class="fa fa-home"></i> Home</a>
    </div>
